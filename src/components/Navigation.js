import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

const Navigation = () => {
    return (

    <div>
        <Layout>
             <Header></Header>
        </Layout>
    </div>
    )
}

export default Navigation;